/* import { TestBed } from '@angular/core/testing';

import { MoviedetailsInterface } from './moviedetails.interface';

describe('MoviedetailsInterface', () => {
  let service: MoviedetailsInterface;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(MoviedetailsInterface);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
}); */
